function applyExtraSetup(sequelize) {
	//insert checks for relationships here

    // eg
    // orchestra.hasMany(instrument);
    // instrument.belongsTo(orchestra);
}

module.exports = { applyExtraSetup };